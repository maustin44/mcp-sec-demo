# This workflow is disabled — replaced by ci.yml (Phase 1) and scheduled-scan.yml (Phase 2)
# Kept as reference only. Do not enable.
name: _disabled_security-scan
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled. See ci.yml and scheduled-scan.yml.'
