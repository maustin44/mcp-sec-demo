# DISABLED — superseded by ci.yml and scheduled-scan.yml
# This workflow referenced agent files that do not exist yet.
# It will be re-enabled and rebuilt in Phase 3 (AI triage agent).
name: _disabled_security-agent
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled until Phase 3'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. AI triage agent implemented in Phase 3.'
